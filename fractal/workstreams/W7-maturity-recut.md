# Workstream PRD: W7-maturity-recut

**Epic:** harness-atlas — the re-cut
**Mode:** **interactive — with KD**
**Dependencies:** W5.
**Routing:** un-routed. Terminal artifact: `fractal/workstreams/W7-maturity-recut-HANDOFF.md`.

## Goal

Un-fuse the two artifacts. The maturity spectrum answers *where are we, what breaks next*; the
primitive catalog answers *what are the parts, who ships one*. After W5 every component carries
`graded:`, so the split is mechanical. This workstream re-points the instrument to it and writes the
forward half of the range argument.

## Deliverable

- `maturity/grid.html` re-pointed: rows are the `graded: true` components only; catalogued
  components appear as a legend, not a ladder. Warp threads match W5's IDs.
- `maturity/00-README.md`: the range argument in one screen — lean-startup to SAFe, indexed to team
  maturity, with the six stages linking down to the graded components each one exercises.
- **The frontier past stage six**, drafted with KD: the lit/dark factory and multiplayer agents.
  Currently unwritten anywhere in the corpus.
- **The latent/deterministic distinction** (attributed to Garry Tan by KD) — **needs a source before
  use.** Find it or mark it as KD's own framing.
- The return path: which pieces come back to LoomWarp as an install-and-config layer, listed, not
  built.

## Acceptance criteria

- **AC-1** Every row in `grid.html` corresponds to a component page with `graded: true`.
- **AC-2** The frontier section exists, is under one screen, and KD wrote or corrected every
  sentence of it.
- **AC-3** No claim is attributed to a named person without a citation.

## Do NOT

- Grade a catalogued component to fill the ladder. Build the LoomWarp install layer here.

---

*Brief: handoff §1, §8.2, §8.4, and the KD ruling on the maturity model in §4.*
